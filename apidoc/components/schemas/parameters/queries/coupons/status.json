{
    "in": "query",
    "name": "status",
    "explode": true,
    "schema": {
        "type": "array",
        "items": {
            "type": "string",
            "enum": [
                "ACTIVE",
                "BLOCKED",
                "HELD",
                "USED"
            ]
        }
    }
}